import { ContractorContracts } from './contractor-contracts.model';

describe('ContractorContracts', () => {
  it('should create an instance', () => {
    expect(new ContractorContracts()).toBeTruthy();
  });
});
